/* ========================================
   文件上传类型（对齐 OpenAPI 契约）
   ======================================== */

/** 上传文件响应（对齐 UploadFileResponse schema） */
export interface UploadFileResponse {
  url: string;
}
