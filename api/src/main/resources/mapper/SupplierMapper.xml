<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aistore.module.supplier.mapper.SupplierMapper">

    <select id="selectSupplierPage" resultType="com.aistore.module.supplier.entity.Supplier">
        SELECT
            id, code, name, address, contact, phone, email, remark, status, deleted,
            created_at, updated_at, created_by, updated_by
        FROM supplier
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                name LIKE CONCAT('%', #{keyword}, '%')
                OR code LIKE CONCAT('%', #{keyword}, '%')
                OR contact LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC, id DESC
    </select>

</mapper>
